rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Only authenticated admins can read/write storage
    match /clients/{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null
        && request.auth.token.admin == true;
    }
  }
}
